<x-form.field>
    <button type="submit"
            class="bg-red-500 text-gray-200 uppercase font-semibold text-xs py-2 px-10 rounded-2xl hover:bg-red-600"
    >
        {{ $slot }}
    </button>
</x-form.field>